{
	"error": {
		"not_found": "Mail queue entry not found"
	},
	"success": {
		"delete": "Mail queue entry / entries deleted"
	},
	"history": {
		"deleted": "Mail queue entry / entries {{entry_ids} removed by user #{{auth_id}}"
	}
}
